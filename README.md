# Material Information

The economics of AI infrastructure, read from the documents. Static site, deployed on Vercel.

Essays live at the repo root as `<slug>.html` and are served at `/essays/<slug>`.
